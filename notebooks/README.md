# Notebook Traceability

`data/memPrjt.ipynb` contains the original exploratory work.

`notebooks/02_train_pipeline.ipynb` is a plain Python training script saved with an `.ipynb` extension in the original project. Its logic has been refactored into the production package under `src/delivery_delay/`.

Use the Streamlit app or `scripts/train.py` for reproducible training.

